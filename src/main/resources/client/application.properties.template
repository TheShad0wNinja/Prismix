# Server connection
server.host=localhost
server.port=8443

# Database configuration
db.name=tavern_client.db

# SSL configuration
ssl.path=client.jks
ssl.password=changeit

# Logging configuration
# Valid levels: TRACE, DEBUG, INFO, WARN, ERROR
logging.level=INFO
logging.file=logs/client.log
